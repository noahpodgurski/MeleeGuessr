import { Character } from "../models/Character";
import { Clip } from "../models/Clip";
import { Player } from "./Player";

export const Clips:Clip[] = [
  {
    clipSrc: "./clips/testClip.mp4",
    player: Player.Armada,
    character: Character.Peach
  }
]